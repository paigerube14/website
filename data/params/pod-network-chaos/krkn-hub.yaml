# Generated by krkn-doc-sync-bot. Do not edit by hand.
source_repo: krkn-hub
source_ref: HEAD
params:
- name: NAMESPACE
  description: Required - Namespace of the pod to which filter need to be applied
  description_source: published-table
  type: string
  default: ''
  required: false
- name: TRAFFIC_TYPE
  description: List of directions to apply filters - egress/ingress ( needs to be a list )
  description_source: published-table
  type: string
  default: '[ingress, egress]'
  required: false
- name: INGRESS_PORTS
  description: Ingress ports to block. Empty list (`[]`) blocks all ingress traffic; otherwise provide port numbers as a bracket list (e.g. `[8443]` or `[22,80]`).
  description_source: published-table
  type: string
  default: '[]'
  required: false
- name: EGRESS_PORTS
  description: Egress ports to block. Empty list (`[]`) blocks all egress traffic; otherwise provide port numbers as a bracket list (e.g. `[8443]` or `[22,80]`).
  description_source: published-table
  type: string
  default: '[]'
  required: false
- name: WAIT_DURATION
  description: The duration (in seconds) that the network chaos (traffic shaping, packet loss, etc.) persists on the target pods. This is the actual time window where the network disruption is active. It must be longer than TEST_DURATION to ensure the fault is active for the entire test.
  description_source: published-table
  type: number
  default: '360'
  required: false
- name: LABEL_SELECTOR
  description: Label of the pod(s) to target
  description_source: published-table
  type: string
  default: ''
  required: false
- name: EXCLUDE_LABEL
  description: Pods matching this label will be excluded from the chaos even if they match other criteria
  description_source: published-table
  type: string
  default: ''
  required: false
- name: POD_NAME
  description: When label_selector is not specified, pod matching the name will be selected for the chaos scenario
  description_source: published-table
  type: string
  default: ''
  required: false
- name: INSTANCE_COUNT
  description: Number of pods to perform action/select that match the label selector
  description_source: published-table
  type: number
  default: '1'
  required: false
- name: TEST_DURATION
  description: Duration of the test run (e.g. workload or verification)
  description_source: published-table
  type: number
  default: '120'
  required: false
- name: SCENARIO_TYPE
  description: Plugin key krkn dispatches the scenario on. Fixed by the container image, not a knob to change.
  description_source: built-in
  default: pod_network_scenarios
  required: false
- name: SCENARIO_FILE
  description: Path to the scenario file baked into the container image. Fixed by the image, not a knob to change.
  description_source: built-in
  default: scenarios/pod_network_scenario.yaml
  required: false
- name: IMAGE
  description: Image used to disrupt network on a pod
  description_source: published-table
  type: string
  default: quay.io/krkn-chaos/krkn:tools
  required: false
