# Generated by krkn-doc-sync-bot. Do not edit by hand.
source_repo: krkn-hub
source_ref: HEAD
params:
- name: NAMESPACE
  description: VMI Namespace to target
  description_source: published-table
  type: string
  default: ''
  required: false
- name: VM_NAME
  description: VMI name to delete, supports regex
  description_source: published-table
  type: string
  default: ''
  required: false
- name: LABEL_SELECTOR
  description: Label selector to target VMs (e.g. app=myvm). Optional if vm-name is set
  description_source: krknctl
  type: string
  default: ''
  required: false
- name: TIMEOUT
  description: Timeout to wait for VMI to start running again, will fail if timeout is hit
  description_source: published-table
  type: number
  default: '60'
  required: false
- name: KILL_COUNT
  description: Number of VMI's to kill (will perform serially)
  description_source: published-table
  type: number
  default: '1'
  required: false
- name: SCENARIO_TYPE
  description: Plugin key krkn dispatches the scenario on. Fixed by the container image, not a knob to change.
  description_source: built-in
  default: kubevirt_vm_outage
  required: false
- name: SCENARIO_FILE
  description: Path to the scenario file baked into the container image. Fixed by the image, not a knob to change.
  description_source: built-in
  default: scenarios/kubevirt_scenario.yaml
  required: false
