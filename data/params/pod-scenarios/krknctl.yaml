# Generated by krkn-doc-sync-bot. Do not edit by hand.
source_repo: krknctl
source_ref: HEAD
params:
- name: NAMESPACE
  description: Targeted namespace in the cluster ( supports regex )
  description_source: krknctl
  type: string
  default: openshift-.*
  flag: namespace
  required: false
- name: NODE_NAMES
  description: 'Name of the node(s) to target. Example: ["worker-node-1","worker-node-2","master-node-1"]'
  description_source: krknctl
  type: string
  default: ''
  flag: node-names
  required: false
- name: NODE_LABEL_SELECTOR
  description: Label selector to target nodes
  description_source: krknctl
  type: string
  default: ''
  flag: node-label-selector
  required: false
- name: POD_LABEL
  description: Label of the pod(s) to target ex. "app=test"
  description_source: krknctl
  type: string
  default: ''
  flag: pod-label
  required: false
- name: EXCLUDE_LABEL
  description: Pods matching this label will be excluded from the chaos even if they match other criteria
  description_source: krknctl
  type: string
  default: ''
  flag: exclude-label
  required: false
- name: NAME_PATTERN
  description: Regex pattern to match the pods in NAMESPACE when POD_LABEL is not specified
  description_source: krknctl
  type: string
  default: .*
  flag: name-pattern
  required: false
- name: DISRUPTION_COUNT
  description: Number of pods to disrupt
  description_source: krknctl
  type: number
  default: '1'
  flag: disruption-count
  required: false
- name: EXECUTION
  description: When multiple pods are targeted, delete them in serial (one at a time) or in parallel (concurrently)
  description_source: krknctl
  type: enum
  default: serial
  flag: execution
  possible_values:
  - serial
  - parallel
  required: false
- name: KILL_TIMEOUT
  description: Timeout to wait for the target pod(s) to be removed in seconds
  description_source: krknctl
  type: number
  default: '180'
  flag: kill-timeout
  required: false
- name: EXPECTED_RECOVERY_TIME
  description: Fails if the pod disrupted do not recover within the timeout set
  description_source: krknctl
  type: number
  default: '120'
  flag: expected-recovery-time
  required: false
- name: FORCE
  description: When true, pods are killed immediately (grace_period_seconds=0) without waiting for graceful termination. When false (default), pods are deleted gracefully respecting terminationGracePeriodSeconds
  description_source: krknctl
  type: boolean
  default: 'false'
  flag: force
  required: false
