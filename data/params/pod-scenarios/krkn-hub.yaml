# Generated by krkn-doc-sync-bot. Do not edit by hand.
source_repo: krkn-hub
source_ref: HEAD
params:
- name: NAMESPACE
  description: Targeted namespace in the cluster ( supports regex )
  description_source: published-table
  type: string
  default: openshift-.*
  required: false
- name: POD_LABEL
  description: Label of the pod(s) to target
  description_source: published-table
  type: string
  default: ''
  required: false
- name: EXCLUDE_LABEL
  description: Pods matching this label will be excluded from the chaos even if they match other criteria
  description_source: published-table
  type: string
  default: ''
  required: false
- name: NAME_PATTERN
  description: Regex pattern to match the pods in NAMESPACE  when POD_LABEL is not specified
  description_source: published-table
  type: string
  default: .*
  required: false
- name: DISRUPTION_COUNT
  description: Number of pods to disrupt
  description_source: published-table
  type: number
  default: '1'
  required: false
- name: EXECUTION
  description: Execution mode for pod deletion (serial or parallel)
  description_source: published-table
  type: enum
  default: serial
  required: false
- name: KILL_TIMEOUT
  description: Timeout to wait for the target pod(s) to be removed in seconds
  description_source: published-table
  type: number
  default: '180'
  required: false
- name: EXPECTED_RECOVERY_TIME
  description: Fails if the pod disrupted do not recover within the timeout set
  description_source: published-table
  type: number
  default: '120'
  required: false
- name: NODE_LABEL_SELECTOR
  description: Label of the node(s) to target
  description_source: published-table
  type: string
  default: ''
  required: false
- name: NODE_NAMES
  description: 'Name of the node(s) to target. Example: ["worker-node-1","worker-node-2","master-node-1"]'
  description_source: published-table
  type: string
  default: ''
  required: false
- name: FORCE
  description: When true, pods are killed immediately (grace_period_seconds=0) without waiting for graceful termination. When false (default), pods are deleted gracefully respecting terminationGracePeriodSeconds
  description_source: krknctl
  type: boolean
  default: 'false'
  required: false
- name: SCENARIO_TYPE
  description: Plugin key krkn dispatches the scenario on. Fixed by the container image, not a knob to change.
  description_source: built-in
  default: pod_disruption_scenarios
  required: false
- name: SCENARIO_FILE
  description: Path to the scenario file baked into the container image. Fixed by the image, not a knob to change.
  description_source: built-in
  default: scenarios/pod_scenario.yaml
  required: false
