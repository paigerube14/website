# Generated by krkn-doc-sync-bot. Do not edit by hand.
source_repo: krkn-hub
source_ref: HEAD
params:
- name: TOTAL_CHAOS_DURATION
  description: Set chaos duration (in sec) as desired
  description_source: published-table
  type: number
  default: '60'
  required: false
- name: IO_BLOCK_SIZE
  description: string size of each write in bytes. Size can be from 1 byte to 4m
  description_source: published-table
  type: string
  default: 1m
  required: false
- name: IO_WORKERS
  description: Number of stressor instances
  description_source: published-table
  type: number
  default: '5'
  required: false
- name: IO_WRITE_BYTES
  description: string writes N bytes for each hdd process. The size can be expressed as % of free space on the file system or in units of Bytes, KBytes, MBytes and GBytes using the suffix b, k, m or g
  description_source: published-table
  type: string
  default: 10m
  required: false
- name: NAMESPACE
  description: Namespace where the scenario container will be deployed
  description_source: published-table
  type: string
  default: default
  required: false
- name: NODE_SELECTOR
  description: defines the node selector for choosing target nodes. If not specified, one schedulable node in the cluster will be chosen at random. If multiple nodes match the selector, all of them will be subjected to stress. If number-of-nodes is specified, that many nodes will be randomly selected from those identified by the selector.
  description_source: published-table
  type: string
  default: ''
  required: false
- name: TAINTS
  description: 'List of taints for which tolerations need to be created. Example: ["node-role.kubernetes.io/master:NoSchedule"]'
  description_source: published-table
  type: string
  default: '[]'
  required: false
- name: NUMBER_OF_NODES
  description: restricts the number of selected nodes by the selector
  description_source: published-table
  type: number
  default: ''
  required: false
- name: NODE_MOUNT_PATH
  description: the local path in the node that will be mounted in the pod and that will be filled by the scenario
  description_source: published-table
  type: string
  default: /root
  required: false
- name: IMAGE
  description: the container image of the stress workload
  description_source: published-table
  type: string
  default: quay.io/krkn-chaos/krkn-hog
  required: false
- name: SCENARIO_TYPE
  description: Plugin key krkn dispatches the scenario on. Fixed by the container image, not a knob to change.
  description_source: built-in
  default: hog_scenarios
  required: false
- name: SCENARIO_FILE
  description: Path to the scenario file baked into the container image. Fixed by the image, not a knob to change.
  description_source: built-in
  default: scenarios/kube/io-hog.yml
  required: false
